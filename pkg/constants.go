package pkg

const SRMQ = "RMQ"
const STargets = "Targets"
const SRedis = "Redis"
const SConfig = "Config"

const SContentTypeOptions = "X-Content-Type-Options"
const SNoSniff = "nosniff"

const Slash string = "/"
